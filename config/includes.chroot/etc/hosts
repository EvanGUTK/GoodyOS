# GoodyOS — base hosts (telemetry block list appended by hook or separate file)
127.0.0.1   localhost
127.0.1.1   goodyos

# Telemetry / AI training / data collection blocks (expand per release)
# OpenAI, Google, Meta, Microsoft, Amazon, Apple — add known endpoints
# See docs for full list; updated with each release.
