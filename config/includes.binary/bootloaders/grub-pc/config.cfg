# GoodyOS — GRUB boot menu config
# Custom theme in boot/grub/theme/

set default=0
set timeout=5
set timeout_style=menu

menuentry "GoodyOS (live)" {
  linux /vmlinuz boot=live quiet
  initrd /initrd.img
}
