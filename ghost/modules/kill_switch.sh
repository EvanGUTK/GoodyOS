#!/usr/bin/env bash
# GoodyOS Ghost — nftables kill switch: if VPN drops, all traffic stops at kernel
# Loads/updates rules from /etc/nftables.conf or inline

set -e
# TODO Phase 6: nftables rules that drop all unless via tunnel
echo "[Ghost] kill_switch: placeholder"
